name: ge_molsg
channels:
  - conda-forge
dependencies:
  - python>=3.9
  # Runtime
  - numpy>=1.24
  - scipy>=1.10
  - scikit-learn>=1.3
  # Optional (progress bars in batch descriptor generation)
  - tqdm>=4.65
  # Development and testing
  - pytest>=8.0
  - coverage>=7.4
  # Install the package itself in editable mode
  - pip
  - pip:
      - -e .
